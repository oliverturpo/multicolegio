# El backend monta la API bajo /api/v1 — el prefijo es obligatorio.
# API del colegio (tenant): apunta al subdominio del colegio (dev: 127.0.0.1 = demo).
VITE_API_URL=http://127.0.0.1:8000/api/v1
# API del panel del dueño (schema public): SIEMPRE el host principal, NO 127.0.0.1.
VITE_PLATAFORMA_API_URL=http://localhost:8000/api/v1
